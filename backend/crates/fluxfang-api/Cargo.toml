[package]
name = "fluxfang-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde_json = "1"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
